version: '3.8'

services:
  manim:
    build: ./manim
    volumes:
      - ./manim:/manim
    working_dir: /manim
    # Run as root to avoid permission issues
    user: root
    environment:
      - GH_TOKEN=${GH_TOKEN}
    command: >
      bash -c "manim -qh solar_system.py GitHubSolarSystem"